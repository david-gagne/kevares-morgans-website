---
import ThankYouVideo from "../images/placeholder.mp4";
---

<style>
  video {
    border-top-right-radius: 0 !important;
  }

  #form-drawer {
    left: 0%;
    /* left: 100%; */
    transition-property: left;
    transition-duration: 250ms;
  }

  span {
    display: block;
    width: 24px;
    height: 2px;
    background-color: var(--dark);
    transition: transform 250ms;
  }

  span::before {
    position: absolute;
    display: block;
    width: 24px;
    height: 2px;
    background-color: var(--dark);
    content: "";
    top: 28px;
    transition: transform 250ms;
  }

  span::after {
    position: absolute;
    display: block;
    width: 24px;
    height: 2px;
    background-color: var(--dark);
    content: "";
    top: 22px;
    transition-property: top, transform;
    transition-duration: 250ms;
  }

  span.isActive {
    background: none;
  }

  span.isActive::before {
    top: 31px;
    transform: rotate(45deg);
  }

  span.isActive::after {
    top: 31px;
    transform: rotate(-45deg);
  }

  .form-container {
    background-color: var(--primary);
    border-top-left-radius: 0;
  }

  fieldset {
    border: none;
    border-left: 5px solid white;
  }

  legend {
    font-family: "Fivo Sans Medium", sans-serif;
  }
</style>

<script>
  let formDrawer = document.getElementById("form-drawer");
  let toggle = document.getElementById("toggle");

  toggle.addEventListener("click", function (event) {
    formDrawer.style.left = "100%";

    setTimeout(() => {
      formDrawer.classList.remove("flex");
      formDrawer.classList.add("dn");
    }, 200);

    document.body.classList.remove("overflow-hidden");
  });
</script>

<div
  id="form-drawer"
  class="w-100 vh-100 bg-white z-999 justify-center pa3 top-0 fixed overflow-scroll dn"
>
  <div class="w-100 mw7 center">
    <div class="w-100 h2">
      <span id="toggle" class="fr isActive"></span>
    </div>

    <h2 class="f3 f2-ns lh-title">Contact Form Modal Title</h2>

    <!-- video -->
    <div>
      <video
        class="w-100 br4"
        controls
        width="268"
        playsinline
        src={ThankYouVideo}></video>
    </div>

    <p class="f4-ns lh-copy">
      The transcript of what Kevares says in the video. Lorem ipsum dolor sit
      amet, consectetur adipisicing elit. Ab saepe mollitia iusto delectus
      consequatur, voluptatum magni similique vero reiciendis consequuntur ipsum
      qui culpa placeat, ducimus ad praesentium molestias aut eius.
    </p>

    <div class="form-container mt4 pa2 white br4 mb5">
      <h3 class="f3 ttu tracked">Let's Talk</h3>
      <form class="pb5" action="">
        <fieldset>
          <legend class="outline-none">Subject</legend>

          <div class="mv3">
            <label class="lh-copy" for="buyer"
              >Buy your next property with me.</label
            >
            <input
              type="radio"
              name="client-relationship"
              id="buyer"
              value="buyer"
            />
          </div>

          <div>
            <label class="lh-copy" for="buyer"
              >Sell your next property with me.</label
            >
            <input
              type="radio"
              name="client-relationship"
              id="seller"
              value="sller"
            />
          </div>
        </fieldset>
      </form>
    </div>
  </div>
</div>
